These examples show different features of Flex GUI

The simple-sim is a bare bones simulator for 3 axes

The images shows how to have an image on a button

The probe is a simulator

The status shows all the status labels

The plc has classicladder
