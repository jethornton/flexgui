
Copy the subroutine .ngc files into /user/linuxcnc/nc_files folder

Copy the images folder into the config folder you put the sim files in

You will need to edit the PROGRAM_PREFIX = path and the SUBROUTINE_PATH = path in the ini
