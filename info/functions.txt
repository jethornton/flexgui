load_file
file_selector
action_open
action_edit
action_reload
action_save
action_save_as
action_edit_tool_table
action_reload_tool_table
action_ladder_editor
action_quit
action_estop
action_power
action_run
action_run_from_line
action_step
action_pause
action_resume
action_stop
action_clear_mdi
action_copy_mdi
action_save_mdi
action_toggle_dro
action_toggle_limits
action_toggle_extents_option
action_toggle_live_plot(parent):
action_toggle_velocity(parent):
action_toggle_metric_units(parent):
action_toggle_program(parent):
action_toggle_rapids(parent):
action_toggle_tool(parent):
action_toggle_lathe_radius(parent):
action_toggle_dtg(parent):
action_toggle_offsets(parent):
action_toggle_overlay(parent):
action_clear_live_plot(parent):
action_show_hal(parent): # actionShow_HAL
action_hal_meter(parent): # actionHal_Meter
action_hal_scope(parent): # actionHal_Scope
action_about(parent): # actionAbout
action_quick_reference(parent): # actionQuick_Reference
startup(parent):
all_create_stylesheet(parent, what):
clear_stylesheet(parent):
to_int(string, default=0):
is_float(string):
is_int(string):
is_number(string):
is_fraction(item):
convert_fraction(item):
is_valid_increment(parent, item): # need to return text ,data and suffix
string_to_int(string):
string_to_float(string):
is_valid_qcolor(color_str):
file_chooser(parent, caption, dialog_type, nc_code_dir=None):
all_homed(parent):
all_unhomed(parent):
home_all_check(parent):
hal_confirm(parent):
jog_toggled(parent):
update_jog_lb(parent):
set_jog_increment(parent, position):
copy_errors(parent):
clear_errors(parent):
clear_info(parent):
ok_for_mdi(parent):
add_mdi(parent): # when you click on the mdi history list widget
update_mdi(parent):
feed_override(parent, value):
rapid_override(parent, value):
max_velocity(parent,value):
update_qcode_pte(parent):
update_start_line(parent):
nc_code_changed(parent):
read_dir(parent): # touch screen file navigator
sync_checkboxes(parent, sender, receiver):
sync_toolbuttons(parent, view):
var_value_changed(parent, value):
sync_var_file(parent, command):
var_file_watch(parent):
io_watch(parent):
update_home_controls(parent):
update_controls(parent):
update_hal_io(parent, value):
update_hal_spinbox(parent, value):
update_hal_slider(parent, value):
change_page(parent):
next_page(parent):
previous_page(parent):
flash_buttons(parent):
update_grid_size(parent, grid_size):
moveEvent(self, event):
resizeEvent(self, event):
qInitResources():
qCleanupResources():
def moveEvent(self, event):
resizeEvent(self, event):
startup(parent):
create_stylesheet(parent):
clear_stylesheet(parent):
next(self):
back(self):
post(self):
clear(self):
backspace(self):
left_arrow(self):
right_arrow(self):
space(self):
retval(self):
moveEvent(self, event):
resizeEvent(self, event):
startup(parent):
create_stylesheet(parent):
clear_stylesheet(parent):
check_dia(self, parent):
check_speed(self, parent):
calculate(self, parent):
change_units(self):
setup_material(self):
read(parent):
qInitResources():
qCleanupResources():
startup(parent):
create_stylesheet(parent):
clear_stylesheet(parent):
save_point_1(self):
save_point_2(self):
save_point_3(self):
calculate_center(self):
move_to_center(self): # FIXME or delete me
set_to_x0y0(self): # FIXME or delete me
update(parent):
startup(parent):
create_stylesheet(parent):
clear_stylesheet(parent):
home(parent):
home_all(parent):
unhome(parent):
unhome_all(parent):
run_mdi(parent, cmd=''):
add_mdi(parent): # when you click on the mdi history list widget
mdi_button(parent):
jog_check(parent):
set_jog_override(parent):
jog(parent): # only do jog check if button is down
jog_selected(parent):
keyboard_jog(parent, action, axis=None, direction=None):
change_cs(parent):
clear_axis_offset(parent, axis):
clear_cs(parent):
tool_change(parent):
touchoff(parent):
tool_touchoff(parent):
spindle_control(parent, spindle, action, value=None):
spindle_override(parent, spindle=0, value=0):
spindle_override_preset(parent, spindle, value):
flood_toggle(parent):
mist_toggle(parent):
optional_stop_toggle(parent):
block_delete_toggle(parent):
feed_override_toggle(parent):
feed_override_preset(parent):
rapid_override_preset(parent):
search.py:	def __init__(self, parent):
search.py:	def find_text(self):
check_cl(self, parent):
check_feed(self, parent):
check_rpm(self, parent):
check_flutes(self, parent):
check_dia(self, parent):
calc_cl(self, parent):
calc_fr(self, parent):
calc_sfm(self, parent):
units(self):
startup(parent):
create_stylesheet(parent):
sub_controls(parent):
clear_stylesheet(parent):
post(self):
clear(self):
space(self):
backspace(self):
left_arrow(self):
right_arrow(self):
change_page(self, index):
retval(self):
moveEvent(self, event):
resizeEvent(self, event):
nextphase(self, unused): pass
progress(self): pass
update(self, count, force=0):
emit_percent(self, percent):
nextphase(self, total):
done(self):
set_text(self, text):
is_lathe(self): return self.lathe_view_option
change_tool(self, pocket):
next_line(self, st):
output_notify_message(self, message):
C(s):
addTimer(self):
poll(self):
showEvent(self, event):
load(self, filename = None):
output_notify_message(self, message):
emit_percent(self, percent):
from_internal_linear_unit(self, v, unit=None):
calculate_gcode_properties(self, canon):
flexplot.py:		def dist(xxx_todo_changeme, xxx_todo_changeme1):
flexplot.py:		def from_internal_units(pos, unit=None):
set_font(self, large=None):
realize(self):
get_font_info(self):
get_program_alpha(self): return self.program_alpha
get_num_joints(self): return self.num_joints
get_joints_mode(self): return self.use_joints_mode
get_show_commanded(self): return self.use_commanded
get_show_extents(self): return self.show_extents_option
get_gcode_properties(self): return self.gcode_properties
get_show_limits(self): return self.show_limits
get_show_live_plot(self): return self.show_live_plot
get_show_machine_speed(self): return self.show_velocity
get_show_metric(self): return self.metric_units
get_show_program(self): return self.show_program
get_show_rapids(self): return self.show_rapids
get_show_relative(self): return self.use_relative
get_show_tool(self): return self.show_tool
get_show_distance_to_go(self): return self.show_dtg
get_grid_size(self): return self.grid_size
get_show_offsets(self): return self.show_offsets
get_view(self):
get_geometry(self):
is_lathe(self): return self.lathe_option
is_foam(self): return self.foam_option
get_current_tool(self):
get_highlight_line(self): return self.highlight_line
get_a_axis_wrapped(self): return self.a_axis_wrapped
get_b_axis_wrapped(self): return self.b_axis_wrapped
get_c_axis_wrapped(self): return self.c_axis_wrapped
set_current_view(self):
clear_live_plotter(self):
winfo_width(self):
winfo_height(self):
activate(self):
deactivate(self):
swapbuffers(self):
_redraw(self):
joint_dro_format_OFF(self,s,spd,num_of_joints,limit, homed):
dro_format(self,s,spd,dtg,limit,homed,positions,axisdtg,g5x_offset,g92_offset,tlo_offset):
minimumSizeHint(self):
sizeHint(self):
normalizeAngle(self, angle):
setXRotation(self, angle):
setYRotation(self, angle):
setZRotation(self, angle):
setZoom(self, zoom):
qglColor(self, color):
initializeGL(self):
paintGL(self):
redraw_perspective(self):
redraw_ortho(self):
basic_lighting(self):
resizeGL(self, width, height):
set_alpha_mode(self, state):
set_inhibit_selection(self, state):
get_plot_colors(self):
get_default_plot_colors(self):
flexplot.py:		def C(s):
set_plot_colors(self, jog=None,traverse=None,feed=None,
set_prime(self, x, y):
select_prime(self, x, y):
select_fire(self):
select_cancel(self, widget=None, event=None):
wheelEvent(self, _event):
mousePressEvent(self, event):
mouseReleaseEvent(self, event):
mouseDoubleClickEvent(self, event):
mouseMoveEvent(self, event):
setScrollMode(self, mode):
panView(self,vertical=0,horizontal=0):
rotateView(self,vertical=0,horizontal=0):
recordCurrentViewSettings(self):
getRecordedViewSettings(self):
setRecordedView(self):
getCurrentViewSettings(self):
presetViewSettings(self,v,z,x,y,lat=None,lon=None):
setView(self,v,z,x,y,lat=None,lon=None):
qss_checkbox.py:def startup(parent):
qss_checkbox.py:def create_stylesheet(parent):
qss_checkbox.py:def clear_stylesheet(parent):
probe.py:def toggle(parent):
qss_toolbar.py:def startup(parent):
qss_toolbar.py:def tbar_create_stylesheet(parent):
qss_toolbar.py:def spacing(parent):
linear_gradient(color):
radial_gradient(x, y, diameter, color):
__init__(self, **kwargs):
paintEvent(self, event):
setLed(self, val):
getLed(self):
__init__(self, **kwargs):
_sync_label_properties(self):
paintEvent(self, event):
setLed(self, val):
getLed(self):
__init__(self, **kwargs):
paintEvent(self, event):
set_led(self, val):
__init__(self, **kwargs):
paintEvent(self, event):
setLed(self, val):
getLed(self):
__init__(self, **kwargs):
paintEvent(self, event):
setLed(self, val):
getLed(self):
save_settings(parent):
get_luminance(color: QColor) -> float:
calculate_contrast_ratio(color1: QColor, color2: QColor) -> float:
startup(parent):
create_stylesheet(parent):
clear_stylesheet(parent):
startup(parent):
create_mw_stylesheet(parent):
clear_stylesheet(parent):
create_fr_stylesheet(parent):
clear_fr_stylesheet(parent):
create_gb_stylesheet(parent):
clear_gb_stylesheet(parent):
spinbox_numbers(parent, obj):
numbers(parent, obj): # obj is the qlineedit that is to receive
gcode(parent, obj):
keyboard(parent, obj):
manual_tool_change(parent):
dialogs.py:	def dia_acc(pin):
touchoff_selected(parent):
tool_touchoff_selected(parent):
find(parent):
select_editor(parent, nc_file):
status_warning(parent, msg):
reset_status_bar(parent):
error_msg_ok(parent, title, msg, info=None): # messagebox.qss added
warn_msg_yes_no(parent, title, msg, info=None): # messagebox.qss added
about_dialog(parent):
quick_reference_dialog(parent):
help_dialog(parent):
def moveEvent(self, event):
def resizeEvent(self, event):
post(self):
clear(self):
dash(self):
left_arrow(self):
right_arrow(self):
backspace(self):
retval(self):
moveEvent(self, event):
resizeEvent(self, event):
set_screen(parent):
setup_vars(parent):
find_widget_index(layout, target_widget):
find_widget_layout(layout, target_widget):
set_hal_enables(parent, obj):
setup_hal_leds(parent):
setup_hal_led_text_labels(parent):
setup_hal_led_labels(parent):
setup_hal_led_buttons(parent):
find_children(parent): # get the object names of all widgets
setup_menus(parent):
setup_actions(parent): # setup menu actions
update_check(parent):
setup_enables(parent):
setup_buttons(parent): # connect buttons to functions
setup_status_labels(parent):
setup_list_widgets(parent):
setup_plain_text_edits(parent):
setup_stacked_widgets(parent):
setup_line_edits(parent):
setup_spin_boxes(parent):
load_postgui(parent): # load post gui hal and tcl files if found
setup_mdi(parent):
setup_mdi_buttons(parent):
setup_jog(parent):
conv_units(value, suffix, units):
setup_spindle(parent):
setup_jog_selected(parent):
setup_touchoff(parent): # touchoff an axis
setup_tool_change(parent):
setup_tool_change_buttons(parent): # Tool Change Buttons
setup_manual_tool_change(parent):
setup_tool_touchoff(parent):
setup_tool_touchoff_selected(parent):
setup_touchoff_selected(parent):
setup_sliders(parent):
setup_overrides(parent): # jog limit switch override checkbox
setup_defaults(parent):
setup_probing(parent):
setup_set_var(parent):
setup_watch_var(parent):
setup_hal(parent):
setup_hal_io_state(parent):
setup_hal_watch(parent):
setup_toolbar(parent):
setup_plot(parent):
setup_fsc(parent): # mill feed and speed calculator
setup_dsf(parent): # drill speed and feed calculator
setup_tpc(parent): # three point center calculator
setup_import(parent):
setup_help(parent):
setup_tabs(parent):
startup(parent):
apply_style(parent):
index_changed(parent, index):

