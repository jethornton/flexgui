name: Build for ARM

on:
  repository_dispatch:
    types: example-event

jobs:
  hello:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Print Hello World
        run: 'echo "Hello World"'